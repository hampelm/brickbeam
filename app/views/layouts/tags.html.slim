= content_for :section_head do
  - if params[:id]
    h2.section-head All #{params[:id]} resources
  - else
    h2 Tags

= content_for :content do
  .tags-panel
    .container
      == yield

= render template: "layouts/application"
